# Научный клубок

https://www.figma.com/board/6dxuChhjXFamLp5o3b1ago/Pipeline-%D0%BE%D0%B1%D1%80%D0%B0%D0%B1%D0%BE%D1%82%D0%BA%D0%B8-%D0%B4%D0%BE%D0%BA%D1%83%D0%BC%D0%B5%D0%BD%D1%82%D0%B0?node-id=0-1&t=g4hYfT5gDdOg05QQ-1

«Научный клубок» — поисково-аналитическая система, которая превращает разрозненные научные документы, таблицы и изображения в связанную базу знаний. Пользователь может задавать исследовательские вопросы обычным языком и получать обоснованные ответы с цитатами, исходными документами и связанными данными из графа знаний.

## Ключевые функции

### Построение графа знаний из документов

Система анализирует загруженные научные документы и извлекает из них материалы, эксперименты, процессы, оборудование, свойства, исследовательские команды, выводы и связи между ними. Новые знания объединяются с уже существующим графом в Neo4j, а синонимы и дубли сопоставляются между собой.

### Анализ текста и визуальных данных

Обрабатывается не только обычный текст, но и таблицы, изображения, графики и схемы из PDF, DOCX, PPTX, XLSX и CSV. Это позволяет использовать сведения, которые при классическом текстовом поиске остались бы незамеченными.

### Исследовательский чат с GraphRAG

Для построения и поиска по графу знаний используется официальная реализация GraphRAG от Neo4j — библиотека `neo4j-graphrag-python`.

Пользователь задаёт вопрос обычным языком, после чего система выполняет гибридный поиск. Он объединяет несколько способов поиска:

- семантический поиск находит фрагменты, близкие к вопросу по смыслу, даже если в них используются другие формулировки;
- полнотекстовый поиск находит точные названия, идентификаторы, термины и числовые значения;
- поиск по графу раскрывает связанные материалы, эксперименты, процессы, свойства, оборудование и документы;
- структурированные фильтры ограничивают результаты по типу сущности, стране, году или числовому условию.

Найденные фрагменты, сущности и связи объединяются, очищаются от повторов и ранжируются по релевантности. В итоговый запрос языковой модели передаётся только наиболее полезный контекст, поэтому система уменьшает количество лишних данных и формирует ответ на основании базы знаний.

### Режим рассуждения

Пользователь может управлять глубиной исследования непосредственно в поле ввода чата:

- **Авто** — система самостоятельно определяет профиль вопроса;
- **Обычный** — используется компактный фактологический поиск с глубиной графа 1 для коротких и конкретных ответов;
- **Исследовательский** — граф раскрывается на глубину до 4 связей для анализа зависимостей, причин, сравнений и противоречий.

Выбранный режим действительно изменяет GraphRAG pipeline: определяет глубину обхода графа, объём связанных сущностей и формат контекста, передаваемого языковой модели. Режим сохраняется между сессиями и отображается вместе с деталями формирования ответа.

### Проверяемые ответы

Исследовательские утверждения сопровождаются цитатами непосредственно в тексте ответа. Пользователь может открыть подтверждающий фрагмент, исходный документ и связанные сущности, а также посмотреть финальный запрос, на основании которого языковая модель сформировала ответ.

Для PDF-документов реализован переход к точному месту источника. При нажатии на цитату система открывает встроенный просмотрщик на нужной странице и подсвечивает ключевые слова из фрагмента, подтверждающего утверждение. Пользователю не требуется самостоятельно искать доказательство по всему документу: путь от ответа языковой модели до оригинального источника занимает один клик.

### Обнаружение проблем в данных

Проблемы обнаруживаются в два этапа. Во время анализа документа языковая модель извлекает явно описанные авторами ограничения: недостаточный объём выборки, отсутствие параметра, противоречие между отчётом и журналом или необходимость дополнительной проверки.

После публикации знаний выполняются автоматические проверки по формальным правилам:

- заполнены ли у эксперимента материал, температура, длительность, оборудование, измеряемое свойство, результат и единица измерения;
- используются ли сопоставимые единицы для одного материала и свойства;
- различаются ли результаты одинаковых экспериментов более чем на 10%;
- существуют ли неисследованные температурные диапазоны шире 50 °C;
- подтверждается ли вывод, процесс или технология независимыми источниками;
- связана ли технология с подтверждающим экспериментом;
- указана ли география применения и не устарела ли публикация.

Для каждой найденной проблемы система определяет тип и уровень важности, создаёт стабильный идентификатор и связывает её с экспериментами, материалами и исходными документами. Благодаря стабильному идентификатору одна и та же проблема не создаётся повторно при каждом запуске проверки.

### Поиск в открытых источниках

Пользователь может явно включить интернет-поиск или попросить выполнить его непосредственно в сообщении. MCP Web Search получает сведения через Yandex Search API, после чего языковая модель формирует ответ с указанием использованных внешних источников.

## Дополнительные функции

- Ответ формируется и отображается в реальном времени.
- История этапов обработки показывает, что система делает с запросом и сколько времени занял каждый этап.
- `CompressionQueryTransformer` преобразует уточняющие вопросы в самостоятельные запросы с учётом истории диалога.
- `RewriteQueryTransformer` оптимизирует длинные пользовательские запросы для поиска по GraphRAG.
- Через символ `@` в контекст чата можно явно добавить материал, документ, эксперимент, проблему или другую сущность графа.
- Граф знаний можно исследовать визуально, переходя между связанными сущностями и исходными документами.
- Материалы, эксперименты, документы и проблемы в данных связаны общей навигацией.
- Перед публикацией пользователь может проверить и отредактировать черновик: изменить тип, название и параметры сущностей, добавить или удалить сущности, а также изменить связи между ними.
- Для обрабатываемого документа отображаются статус, подробный прогресс и прошедшее время; обработку можно отменить.
- Оригинальный документ сохраняется и доступен для скачивания.
- Документ можно добавить не только файлом, но и ссылкой на HTML-статью, PDF, DOCX, PPTX, XLSX или CSV; система сохраняет автора, дату публикации и исходный URL.
- Результат извлечения и найденные проблемы можно скопировать в формате JSON для анализа и отладки.
- Интерфейс поддерживает Markdown, таблицы и форматирование ответов языковой модели.
- Для ответа отображаются время обработки, использованные токены и источники.
